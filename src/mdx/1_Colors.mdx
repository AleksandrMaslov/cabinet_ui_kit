import { Meta, ColorPalette, ColorItem } from '@storybook/blocks'
import { COLORS } from '../constants.ts'

<Meta title="Color Palette" />

## Brand Color Palette

<ColorPalette>
  {Object.values(COLORS).map(({ ...props }) => (
    <ColorItem {...props} />
  ))}

</ColorPalette>
