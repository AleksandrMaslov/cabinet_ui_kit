import { Meta, Typeset } from '@storybook/blocks'
import { typography, sampleText } from './typography'

<Meta title="Typography" />

# Typography

## Primary

**Font:** PT Root UI

**Weights:** 300(Light), 400(Regular), 500(Medium)

<Typeset
  fontSizes={[
    Number(typography.size.primary.s1),
    Number(typography.size.primary.s2),
    Number(typography.size.primary.s3),
    Number(typography.size.primary.m1),
    Number(typography.size.primary.m2),
    Number(typography.size.primary.m3),
    Number(typography.size.primary.l1),
    Number(typography.size.primary.l2),
    Number(typography.size.primary.l3),
  ]}
  fontWeight={typography.weight.primary.medium}
  sampleText={sampleText}
  fontFamily={typography.type.primary}
/>

## Headers

**Font:** Jost

**Weights:** 500(Medium), 700(Bold), 800(ExtraBold)

<Typeset
  fontSizes={[
    Number(typography.size.headers.s1),
    Number(typography.size.headers.s2),
    Number(typography.size.headers.s3),
    Number(typography.size.headers.m1),
    Number(typography.size.headers.m2),
    Number(typography.size.headers.m3),
    Number(typography.size.headers.l1),
    Number(typography.size.headers.l2),
    Number(typography.size.headers.l3),
  ]}
  fontWeight={typography.weight.extraBold}
  sampleText={sampleText}
  fontFamily={typography.type.headers}
/>
